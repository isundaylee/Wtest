/*NG_APDiscoveryResult = function() {
	var this.apInfo = new Array();

	var this.addApInfo: function(apIp, apMAC, apName, apUserName, apPassword) {
		this.apInfo[this.apInfo.length] =
			new NG_WNC_ApInfo(apIp, apMAC, apName, apUserName, apPassword);
	}

	var this.getApInfoByIp(apIp) {
		var apInfo = null;

		for(i = 0; i < this.apInfo.length; i++) {
			if(this.apInfo[i].apIp = apIp) {
				apInfo = this.apInfo[i];
				break;
			}
		}

		return apInfo;
	}
}

NG_WNC_ApInfo = function(apIp, apMAC, apName, apModel, apUserName, apPassword) {
	var this.apIp          = apIp;
	var this.apMAC         = apMAC;
	var this.apName        = apName;
	var this.apModel       = apModel;
	var this.apUserName    = apUserName;
	var this.apPassword    = apPassword;
}*/